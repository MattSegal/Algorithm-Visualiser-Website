module.exports = {
  CHART_HEIGHT: 450, // pixels

  // Number of bars in chart
  INITIAL_BARS: 60,
  MAX_BARS: 100,
  MIN_BARS: 10,
  BARS_INCREMENT: 10,

  // Max value of each bar
  INITIAL_VAL: 100,
  MAX_VAL: 200,
  MIN_VAL: 10,
  VAL_INCREMENT: 10,

  // Sorting render delay (ms)
  INITIAL_DELAY: 80,
  MAX_DELAY: 220,
  MIN_DELAY: 40,
  DELAY_INCREMENT: 20,
}